# Changelog

## Unreleased

- Hardened the repository for standalone public use by moving runtime coupling to environment variables, removing hardcoded internal paths and hosts, and adding packaging metadata for `pip install .`.

## v1.2.1 - 2026-05-30

- Added the session-first `/ui/` browser surface.
- Added session-scoped notify support through the API.
- Expanded project summaries so the UI can render task details without extra write endpoints.
